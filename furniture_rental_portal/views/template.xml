<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <template id="portal_my_home_menu_rental" name="Portal layout : rental menu entries" inherit_id="portal.portal_breadcrumbs" priority="20">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'rental'" class="breadcrumb-item active">
                <a t-attf-href="/my/rental_orders?{{ keep_query() }}">Rental Orders</a>
            </li>
        </xpath>
    </template>
    <template id="portal_my_home_rental" inherit_id="portal.portal_my_home">
        <xpath expr="//div[@id='portal_common_category']/t" position="after">
            <t t-call="portal.portal_docs_entry">
                <t t-set="icon" t-value="'/sale/static/src/img/bag.svg'"/>
                <t t-set="title">Rental</t>
                <t t-set="text">Follow, view or pay your Rental orders</t>
                <t t-set="url" t-value="'/my/rental_orders'"/>
                <t t-set="config_card" t-value="True"/>
            </t>
        </xpath>
    </template>

    <template id="portal_my_rental_orders" name="My Rental Orders">
        <!-- <div id="rental_order_update"> -->
            <t t-call="portal.portal_layout" id="rental_order_update">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Your Rental orders</t>
            </t>
            <div t-if="not orders" class="alert alert-warning" role="alert">
                There are currently no rental orders for your account.
            </div>
            <t t-if="orders" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>
                            <span class='d-none d-md-inline'>Rental Order #</span>
                            <span class='d-block d-md-none'>Ref.</span>
                        </th>
                        <th class="text-center">Order Date</th>
                        <th class="text-center"/>
                        <th class="text-center">Total</th>
                        <th class="text-center">Status</th>
                        
                    </tr>
                </thead>
                <t t-foreach="orders" t-as="order">
                    <tr>
                        <td><a t-att-href="order.get_portal_url()"><t t-out="order.name"/></a></td>
                        <td class="text-center">
                            <span t-field="order.date_order" t-options="{'widget': 'date'}"/>&amp;nbsp;
                            <span class='d-none d-md-inline' t-field="order.date_order" t-options="{'time_only': True}"/>
                        </td>
                        <td class="text-center">
                            <span t-if="order.locked"  class="badge rounded-pill text-bg-success">
                                <i class="fa fa-fw fa-check" role="img" aria-label="Done" title="Done"/>Done
                            </span>
                        </td>
                        <td class="text-center"><span t-field="order.amount_total"/></td>
                        <td class="text-center"><span t-esc="order.rental_status.capitalize()"/></td>
                        <t t-if="order.rental_status == 'orderd'">
                            <td>
                                <a role="button" t-att-href="'/update/rental_order_pickup_state?order_id=%s' % (order.id) " class="btn btn-link p-0"><i class="fa fa-truck"></i>Pickup</a>
                            </td>
                        </t>
                        <t t-if="order.rental_status == 'pickup'">
                            <td>
                                <a role="button" t-att-href="'/update/rental_order_return_state?order_id=%s' % (order.id) " class="btn btn-link p-0"><i class="fa fa-undo"></i>Return</a>
                            </td>
                        </t>
                    </tr>
                </t>
            </t>
        </t>
        <!-- </div> -->
    </template>

    <template id="sale_order_portal_content_extended" inherit_id="sale.sale_order_portal_content">
        <xpath expr="//div[@id='introduction']" position="replace">
            <t t-if="sale_order.is_rental_order">
                <div id="introduction" t-attf-class="#{'border-bottom-0 pt-0 pb-3 bg-white' if report_type == 'html' else ''}">
                    <div class="row" id="intro_row">
                        <h2 class="my-0 col-12 col-lg flex-grow-1 mb-1 mb-lg-0">
                            Rental Order -
                            <em t-out="sale_order.name"/>
                        </h2>
                    </div>
                </div>
            </t>
            <t t-else="">
                <div id="introduction" t-attf-class="#{'border-bottom-0 pt-0 pb-3 bg-white' if report_type == 'html' else ''}">
                    <div class="row" id="intro_row">
                        <h2 class="my-0 col-12 col-lg flex-grow-1 mb-1 mb-lg-0">
                            <t t-out="sale_order.type_name"/> -
                            <em t-out="sale_order.name"/>
                        </h2>
                    </div>
                </div>
            </t>
            
        </xpath>
        <xpath expr="//span[@id='sale_info_title']" position="replace">
            <t t-if="sale_order.is_rental_order">
                <span id="sale_info_title">
                    <h4 class="mb-1">Rental Information</h4>
                    <hr class="mt-1 mb-2"/>
                </span>
            </t>
            <t t-else="">
                <span id="sale_info_title">
                    <h4 class="mb-1">Sale Information</h4>
                    <hr class="mt-1 mb-2"/>
                </span>
            </t>
        </xpath>
        <xpath expr="//table[@id='sales_order_table']" position="replace">
            <t t-if="sale_order.is_rental_order">
                <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                    <thead class="bg-100">
                        <tr>
                            <th class="text-start" id="product_name_header">Products</th>
                            <th class="text-start" id="line_start_date">Start Date</th>
                            <th class="text-start" id="line_end_date">End Date</th>
                            <th class="text-end" id="product_qty_header">Quantity</th>
                            <th t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                Unit Price
                            </th>
                            <th t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <th t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes_header">
                                <span>Taxes</span>
                            </th>
                            <th class="text-end" id="subtotal_header">
                                <span>Amount</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">

                        <t t-set="current_subtotal" t-value="0"/>
                        <t t-set="lines_to_report" t-value="sale_order._get_order_lines_to_report()"/>

                        <t t-foreach="lines_to_report" t-as="line">

                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>

                            <tr t-att-class="'bg-200 fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic o_line_note' if line.display_type == 'line_note' else ''">
                                <t t-if="not line.display_type">
                                    <td id="product_name">
                                        <span t-field="line.name"/>
                                    </td>
                                    <td id="line_st_date">
                                        <span t-field="line.rental_start_date"/>
                                    </td>
                                    <td id="line_ed_date">
                                        <span t-field="line.rental_end_date"/>
                                    </td>

                                    <td class="text-end" id="quote_qty_td">
                                        <div id="quote_qty">
                                            <span t-field="line.product_uom_qty"/>
                                            <span t-field="line.product_uom"/>
                                        </div>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-end'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-out="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-out="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes">
                                        <span t-out="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                    </td>
                                    <td t-if="not line.is_downpayment" class="text-end" id="subtotal">
                                    <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>
                            <tr t-if="current_section and (line_last or lines_to_report[line_index+1].display_type == 'line_section') and not line.is_downpayment"
                                class="is-subtotal text-end">
                                <td colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span t-out="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": sale_order.currency_id}'
                                    />
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
            <t t-else="">
                <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                    <thead class="bg-100">
                        <tr>
                            <th class="text-start" id="product_name_header">Products</th>
                            <th class="text-end" id="product_qty_header">Quantity</th>
                            <th t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                Unit Price
                            </th>
                            <th t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <th t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes_header">
                                <span>Taxes</span>
                            </th>
                            <th class="text-end" id="subtotal_header">
                                <span>Amount</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="sale_tbody">

                        <t t-set="current_subtotal" t-value="0"/>
                        <t t-set="lines_to_report" t-value="sale_order._get_order_lines_to_report()"/>

                        <t t-foreach="lines_to_report" t-as="line">

                            <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>

                            <tr t-att-class="'bg-200 fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic o_line_note' if line.display_type == 'line_note' else ''">
                                <t t-if="not line.display_type">
                                    <td id="product_name">
                                        <span t-field="line.name"/>
                                    </td>
                                    <td class="text-end" id="quote_qty_td">
                                        <div id="quote_qty">
                                            <span t-field="line.product_uom_qty"/>
                                            <span t-field="line.product_uom"/>
                                        </div>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-end'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-out="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-out="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <td t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}" id="taxes">
                                        <span t-out="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                    </td>
                                    <td t-if="not line.is_downpayment" class="text-end" id="subtotal">
                                    <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>
                            <tr t-if="current_section and (line_last or lines_to_report[line_index+1].display_type == 'line_section') and not line.is_downpayment"
                                class="is-subtotal text-end">
                                <td colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span t-out="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": sale_order.currency_id}'
                                    />
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
        </xpath>
    </template>
</odoo>