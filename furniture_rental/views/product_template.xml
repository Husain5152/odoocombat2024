<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- model.name inherit form view -->
    <record id="product_template_from_view_extended" model="ir.ui.view">
        <field name="name">product.template.inherit.view.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='options']" position='inside'>
                <span class="d-inline-block">
                    <field name="is_rental"/>
                    <label for="is_rental"/>
                </span>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page name="rental_rate" string="Rental Rate" invisible="not is_rental">
                    <group>
                        <field name="ranting_rate"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Product Action-->
    <record id="rental_product_action" model="ir.actions.act_window">
        <field name="name">Products</field>
        <field name="res_model">product.template</field>
        <field name="view_mode">kanban,tree,form,activity</field>
        <field name="search_view_id" ref="product.product_template_search_view"/>
        <field name="domain">[('is_rental', '=', True)]</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            No product found. Let's create one!
          </p><p>
            You must define a product for everything you sell or purchase,
            whether it's a storable product, a consumable or a service.
          </p>
        </field>
    </record>

    <!-- Product menu-->
    <menuitem id="rental_products_menu" name="Products" parent="rental_base_menu" action="rental_product_action" sequence="2"/>
</odoo>